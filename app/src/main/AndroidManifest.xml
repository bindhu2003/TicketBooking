<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlineticketbookingapp">

    <application
        android:allowBackup="true"
        android:label="Online Ticket Booking"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineTicketBookingApp">

        <!-- Booking confirmation activity -->
        <activity android:name=".BookingConfirmationActivity" />
        <!-- Restaurant booking activity -->
        <activity android:name=".RestaurantBookingActivity" />
        <!-- Tourist places booking activity -->
        <activity android:name=".TouristBookingActivity" />
        <!-- Theatre booking activity -->
        <activity android:name=".TheatreBookingActivity" />
        <!-- Temple booking activity -->
        <activity android:name=".TempleBookingActivity" />
        <!-- Home activity -->
        <activity android:name=".HomeActivity" />

        <!-- Main activity with explicit android:exported -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
